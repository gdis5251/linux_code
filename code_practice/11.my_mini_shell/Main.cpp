#include <iostream>
#include <cstdlib>
#include <unistd.h>
#include <sys/wait.h>

int Split(char input[], char* output[])
{

}

int main(void)
{
    //1. 打印提示符，并且让用户输入指令
    
    //2. 用户输入一个指令
    
    //3. 解析指令，把要执行哪个程序识别出来，哪些是命令行参数识别出来（字符串切分）
    //切分结果应该是字符串数组

    //4. 创建子进程并且进行程序替换
    
    return 0;
}
